import React from 'react';
import Header from '../../../Shared/Header/Header';

const Login = () => {
    return (
        <div>
            <Header/>
            <h1>This is login</h1>
        </div>
    );
};

export default Login;